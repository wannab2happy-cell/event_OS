export const dynamic = 'force-dynamic';

export default function StaffLayout({ children }: { children: React.ReactNode }) {
  // Staff 레이아웃은 각 [eventId] 페이지에서 StaffLayoutShell을 사용
  // 여기서는 기본 구조만 제공
  return <>{children}</>;
}

